import React from 'react';
import { View, Text } from 'react-native';
import { CaprinManagementProps } from '../../../../types/navigations';

const CaprinManagement: React.FC<CaprinManagementProps> = ({ navigation }) => {
  return (
    <View>
      <Text>Caprin Management Screen</Text>
    </View>
  );
};

export default CaprinManagement;